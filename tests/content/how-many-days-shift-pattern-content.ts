export const headings = {
    holidayEntitlement : `Calculate holiday entitlement`,
    daysShiftPattern : `How many days in the shift pattern?`
}

export const hint = {
    daysShiftPattern: `The shift pattern includes non-working days. `
}

export const inputForm = {
    selector :`#response`
}